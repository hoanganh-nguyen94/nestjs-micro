kind: Service
apiVersion: v1
metadata:
  name: recipe-svc
  labels:
    app: recipe-svc
spec:
  type: ClusterIP
  selector:
    app: recipe-svc
  ports:
    - port: 50052
      targetPort: 50052
      protocol: TCP
