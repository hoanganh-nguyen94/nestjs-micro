apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
#    nginx.ingress.kubernetes.io/rewrite-target: /$2
#    nginx.ingress.kubernetes.io/enable-cors: "true"
#    nginx.ingress.kubernetes.io/cors-allow-methods: "PUT, GET, POST, OPTIONS"
#    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
#    nginx.ingress.kubernetes.io/cors-allow-origin: "http://simple.ui"
#    nginx.ingress.kubernetes.io/configuration-snippet: |
#      if ($http_x_forwarded_proto = 'http') {
#        return 301 https://$host$request_uri;
#      }
#      more_set_headers "Access-Control-Allow-Headers: Sentry-Trace,DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization";
  name: gateway-svc
spec:
  rules:
    - host: "api.simple.ui"
      http:
        paths:
          - path: /
            backend:
              serviceName: gateway-svc
              servicePort: 3000
