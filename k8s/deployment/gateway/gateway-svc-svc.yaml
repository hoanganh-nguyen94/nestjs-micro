apiVersion: v1
kind: Service
metadata:
  name: gateway-svc-v1
  namespace: demo
  labels:
    app: gateway-svc
spec:
  type: ClusterIP
  selector:
    app: gateway-svc
    version: v1
  ports:
    - protocol: TCP
      name: http
      port: 3000
      targetPort: 3000
