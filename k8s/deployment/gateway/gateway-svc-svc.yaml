apiVersion: v1
kind: Service
metadata:
  name: gateway-svc
  labels:
    app: gateway-svc
    service: gateway-svc
spec:
  type: ClusterIP
  selector:
    app: gateway-svc
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP


